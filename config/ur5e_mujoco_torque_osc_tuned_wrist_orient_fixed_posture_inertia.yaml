# TESTED 2026-08-05 -- REGRESSES. DO NOT USE without new evidence.
# A/B result: canonical grid 9/24 -> 3/24 valid move-and-hold; NO improvement at the -45deg
# pose this was designed for (0/24 both arms). Failures are hold-phase X-drift misses, not
# safety trips (no guard fired first in any changed cell). A 2-point kp_posture/kd_posture
# sensitivity check scaled by mean(diag(M)) recovered nothing.
# Full evidence: docs/status/posture_inertia_scaling_ab_2026-08-05.md
# Kept as a reproducible record of a negative result, not as a usable config.
