[pytest]
testpaths = tests
markers =
    unit: pure-numpy controller_core unit tests (no simulator required)
    mujoco: tests requiring the mujoco python package
    hardware: tests exercising the hardware staging lane (mocked RTDE; no robot required)
    slow: long-running tests (excluded via -m "not slow")
